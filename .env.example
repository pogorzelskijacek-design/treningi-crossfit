# Supabase — copy to .env.local and fill in from your Supabase project
# (Dashboard → Project Settings → API). The anon key is public-safe (protected
# by Row-Level Security). Leave these unset to run the app offline/local-only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
